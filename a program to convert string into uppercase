#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str = "hello";
    for(int i=0;i<str.size();i++)
    {
        if(str[i]>='a' && str[i]<='z')
        {
            str[i] = str[i] - 32;
        }
        cout<<str[i];
    }
    return 0;
}
